/* exported isUpperCased */

function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  }
  return false;
}

/*
1. check if word is uppercase
2. if word is uppercase, return true
3. if word is not uppercase, return false
*/
